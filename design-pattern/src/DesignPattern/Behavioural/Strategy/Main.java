package DesignPattern.Behavioural.Strategy;
/*
It is used when there is a behaviour and there multiple variants of it based on some parameter.
So we create an interface which have that behaviour and classes which implements the interface.
We also have a factory which returns the object of a class based on a parameter and finally used the object to call the behaviour method.
 */
public class Main {}
