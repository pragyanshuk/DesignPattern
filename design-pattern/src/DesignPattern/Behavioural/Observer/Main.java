package DesignPattern.Behavioural.Observer;
/*
It is used when u have a class which have an interesting event and there are multiple classes which needs to be notified when the event is triggered
The class which have an interesting event and whose job is to notify other classes is called the Publisher or Producer class.
The classes which needs to be notified when the event is triggered are called the Subscriber/Listener class.
 */
public class Main {

}
