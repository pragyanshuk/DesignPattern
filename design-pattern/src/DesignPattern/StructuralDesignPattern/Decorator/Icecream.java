package DesignPattern.StructuralDesignPattern.Decorator;
//Step 1 - Create interface of a base class over which u have to add decorator/functionality/
public interface Icecream {
    int getCost();
    String getConstituents();
}
